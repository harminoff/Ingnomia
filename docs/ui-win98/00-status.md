# Windows 98 UI implementation status

Updated: 2026-09-25

Stage 08 Inventory is implemented and verified for automated scope: 190 assertions, 10/10 focused tests, rebuilt Stage 06/07 regressions, four-scale production captures and copied-save data checks. See the [Stage 08 reference report](stages/08-inventory.md). Stage 09 Stockpiles is implemented and verified for automated scope: 90 assertions, 10/10 focused tests, earlier form/report regressions, four-scale captures and authoritative copied-save rule/template/suspension checks. See [Stage 09](stages/09-stockpiles.md).

Stage 10 Workshops is implemented and verified for automated scope as a Windows 98 property sheet (OK/Cancel/Apply, non-scrolling pages, documented controls), with the Windows 98 caption on all six managers: 125 checks, 10/10 focused tests, rebuilt Stage 04-09 regressions, and four production runs at 100/125/150/200% (40/40 live checks each, all 21 save hashes unchanged). See [Stage 10](stages/10-workshops-trade.md).

Stage 11 Agriculture is implemented and verified for automated scope as a Windows 98 property sheet for farms, groves and pastures (pending settings with OK/Cancel/Apply, a list-view plot grid with the documented selection model, per-record animal and food targets, no unimplemented Priority), plus a shared Windows 98 message box: 407 checks, 10/10 focused tests, Stage 04-10 regressions, and four production runs at 100/125/150/200% (59/59 live checks each, all 21 save hashes unchanged). See [Stage 11](stages/11-agriculture.md). Windows 98 design citations for the remaining stages are in [07-win98-design-reference.md](07-win98-design-reference.md).

Stage 12 Population is implemented and verified for automated scope as a Windows 98 property sheet (Citizens/Skills/Professions/Schedules, Close only): sortable list view roster with selection kept by ID, per-citizen and bulk skill scope, the two-list profession editor, Windows 98 message boxes: 189 checks, 10/10 focused tests, Stage 04-11 regressions, and four production runs at 100/125/150/200% (32/32 live checks each, all 21 save hashes unchanged). See [Stage 12](stages/12-population-professions.md).
Stage 13 Schedules is implemented and verified for automated scope as an Excel 97 style grid (frozen headings, active cell, cell/day/hour/range selection, exact scope preview and review): 115 checks, 10/10 focused tests, Stage 04-12 regressions, and four production runs at 100/125/150/200% (24/24 live checks each, all 21 save hashes unchanged). See [Stage 13](stages/13-schedules.md).
Stage 14 Military is implemented and verified for automated scope as a Windows 98 property sheet (Squads/Members/Roles/Uniforms/Targets, Close only): explicit squad/citizen/role/destination flow, civilian check box, legal uniform choices with stated scope, target ordering separate from the response, named deletion message boxes: 282 checks, 13/13 ctest, Stage 04-13 regressions, and four production runs at 100/125/150/200% (45/45 live checks each, all 21 save hashes unchanged). See [Stage 14](stages/14-military.md).
Stage 15 Diplomacy is implemented and verified for automated scope as a Windows 98 property sheet (Neighbors/Missions, Close only, Unknown facts) with a Send Mission wizard (Mission/Citizen/Review, Back/Next/Finish/Cancel, revalidated single send): 184 checks, 13/13 ctest, Stage 04-14 regressions, and four production runs at 100/125/150/200% (23/23 live checks each, all 21 save hashes unchanged). See [Stage 15](stages/15-diplomacy.md).
Stage 16 Inspectors is implemented and verified for automated scope. The creature and object inspectors are Windows 98 palette windows: property inspectors with Close only, Unknown values and tabs. Crowded tiles are chosen by ID, and Population Properties opens the one creature inspector (POP-05). Results: 408 checks, 9/9 ctest, Stage 04-15 regressions, and four production runs at 100/125/150/200% (33/33 live checks each, all 21 save hashes unchanged). See [Stage 16](stages/16-inspectors.md).
Stage 17 HUD is implemented and verified for automated scope. The game window has a Windows 98 toolbar with drop-down menus and a status bar, and armed tools show the option-set button, the status bar mode and a map-only cross pointer. Build is a 384 x 380 palette, the tutorial is a palette, and events use message boxes. Results: 222 checks, 7/7 ctest, Stage 04-16 regressions, and four production runs at 100/125/150/200% (46/46 live checks each, all 21 save hashes unchanged). See [Stage 17](stages/17-hud.md).
Stage 18 Main menu, new game and saves is implemented and verified for automated scope. The main menu is a Windows 98 dialog box, Custom Game is a wizard, and Load Game follows the Open dialog. Along the way it fixed NEW-002, a list rebuilt under its own click, a request left pending from start-up that disabled Open and Finish, and overlays taking the focus. Results: 199 checks, 4/4 ctest, Stage 04-17 regressions, and four production runs at 100/125/150/200% from the main menu (33/33 live checks each, all 21 save hashes unchanged). See [Stage 18](stages/18-main-menu-new-game-saves.md).
Stage 19 Settings, pause and loading is implemented and verified for automated scope. Settings is a Close-only property sheet, Pause is a dialog over the paused map, and Loading is a message box. Main Menu and Exit use Yes / No Win98 message boxes. NEW-004 (crash on a missing or damaged save) and a pending request that kept Pause unavailable after a load are fixed. Results: 144 checks, 4/4 ctest, Stage 04-18 regressions, four production runs (34/34 each), and missing and truncated save runs without a crash. See [Stage 19](stages/19-settings-pause-loading.md).
Stage 20 Scaling, keyboard and accessibility is implemented and verified for automated scope. It adds access keys (Alt and plain letters) on the shell dialogs, wizard, Settings, Load Game and message boxes, Ctrl+Page Down/Up on tabs, Windows High Contrast support in every document, remembered palette positions, a 640 x 480 fit, and a title for every window. Results: 182 checks, 4/4 ctest, Stage 04-19 regressions, and five production runs (17/17 each). See [Stage 20](stages/20-scaling-keyboard-accessibility.md).
Stage 21 Cleanup and conformance is implemented and verified for automated scope. It migrates the Stockpile window to a property sheet and the Inventory window to a report window with Item Properties, keys every Windows 98 string with its access key in the catalog, fixes the stale foundation contracts, removes dead styles and keys, and records the conformance checklist. Results: Stage 21 suite 1971 checks, rewritten Stage 08 and 09 suites, all regressions, canonical ctest 13/13, and live runs at four scales. See [Stage 21](stages/21-cleanup-conformance.md).










Population crash review: all four recent dumps identified Stage 07 tooltip hover recursion. Fixed and rebuilt; 179 Stage 07 assertions, Stage 06 regressions, and production pointer stress at 100%/200% pass. [Crash review and limits](stages/07-population-crash-review.md). Stage 08 proceeded after this crash correction was verified.

## Review completed for shared automated scope: Stages 01-03

The review found and corrected responsive gallery failures, missing block/table defaults, inline-template ownership, unsupported arrow glyphs, title truncation/recovery and initial empty-title handling. It also corrected stale source contracts and overstated evidence claims. Four final scale runs pass the strengthened production probe. Inventory, Settings and a stockpile inspector were rechecked through production bindings. See [review findings and exact limits](stages/01-03-review.md).

Native physical input remains unverified. Stage 04 is verified for shared controls and the New Game pilot: 4/4 focused tests (82 Stage 04 assertions), 167 Rml plus 13 Qt-host assertions, and eight production capture runs pass. Manager tab migrations remain assigned to their feature stages. See the [Stage 04 checkpoint](stages/04-buttons-tabs.md).

## Checkpoint

Stage 00 is VERIFIED for baseline scope. The source checkout and production build are known, the route registry and 98-item traceability table are recorded, and the application has been launched with isolated fixture data, copied Tutorial Valley saves, and a generated disposable world. The tutorial save was generated through the production save command, reloaded, copied, and loaded with renderer captures. Main menu, New Game, Load Game, both Settings entry points, Pause, Inventory, Population, Military, Diplomacy, a live tile inspector, a one-plot Farm Overview, a scoped two-of-four-plot crop plan, a populated Stockpile manager, the detached Stockpile filter popup, and the typed Exit confirmation have production-route evidence; the live creature inspector has Camera, Stats, and Expertise captures. The Workshop manager also opened against an in-memory Carpenter model, though the normal build workflow and production state remain unverified. The tile probe passed 28 assertions, including stockpile deletion with item preservation and authoritative Remove floor job creation/cancel. Stockpile and Farm runs used fresh copies whose 21 save hashes remained unchanged. A live event-prompt modal was repeated on two disposable copies. The Inventory report's Space watch shortcut now has an end-to-end trace from focused row through `watch.set` to a newer authoritative snapshot. A production shell click trace changed main menu to New Game and returned through Back; focus returned to `shell-load` rather than the invoking `shell-new-setup` and is tracked as NEW-002 for Stage 18. The separately supplied audit and triage index resolved the six-risk identification gap; source dispositions are recorded in the Stage 00 checkpoint. The route coverage report records each route's evidence and remaining prerequisites. Workshop normal construction/production, Stockpile filter/scope/settings behavior, Agriculture Farm settings/harvest/job execution, Grove/Pasture/empty states, and coherent Workshop Priority/draft-target behavior remain assigned to their downstream feature stages. Baseline traces now cover a Workshop Name draft and Apply, a two-of-four Farm crop-plan edit, typed Exit cancel/confirm, and an open Stockpile filter popup. Shell focus restoration remains NEW-002 for Stage 18; physical input is unavailable in this session.

Stage 01 is VERIFIED: the design-system verifier checks active styles and actual RmlUi scrollbar part names; the responsive full-viewport component gallery renders through the production host twice, and an in-process probe confirms RmlUi-generated select and scrollbar children. Repeated production sentinels cover Inventory, Stockpile, two-of-four Farm scope, Settings, inspector states, and modal routes. The registered suite remains 11/13 with the same two unrelated checkout-level failures recorded at Stage 00.

Stage 02 is VERIFIED for shared theme ownership and cascade. Shared color roles now have one generated token source, deterministic CMake generation, and accessibility styles load after route styles. The final production matrix captured all 21 supported surfaces successfully; the extra `kingdom_panel` automation probe still cannot activate its configured element ID. Route-owned colors remain assigned to their owning later stages. See [Stage 02 checkpoint](stages/02-theme.md) and [evidence manifest](evidence/stage-02-theme-manifest.txt).

Stage 03 shared chrome implementation is in place and its static component gallery passed production-renderer layout checks at 100%, 125%, 150%, and 200%. The design-system verifier and current production build passed. Shared active/inactive captions, 64×22dp title-bar Close targets, tokenized type roles, original CSS glyph geometry, and group/content regions are documented in [Stage 03](stages/03-chrome.md). Full physical hover, close, host-switch, and main-window resize checks remain open; route-local select and scrollbar arrows are assigned to Stages 05–06.

## Preserved checkout state

- Active repository: `Ingnomia/`, branch `feat/separate-ui-windows`, HEAD `bca0bee3acf79d460360c5f6b96fde08db784585`.
- This HEAD matches the revision named in the embedded historical audit.
- The pre-existing modification `docs/layer-scroll-review-2026-09-13.md` is untouched.
- The outer `Ingnomia2` repository has its own pre-existing changes; this work is confined to the nested `Ingnomia/` checkout.

## Completed evidence

- `Ingnomia.exe` built successfully in `RelWithDebInfo` after invoking the Visual Studio 2026 x64 developer environment.
- The production executable launched with a unique temporary data folder. It opened the primary shell and a separate RmlUi Inventory window populated by `showInventoryFixture()`; the generated inventory is explicitly fixture data.
- Created a five-gnome Tutorial Valley world in an isolated `%TEMP%` data root, saved it to `TutorialValley/1` through `EventConnector::onSaveGame()`, ended and reloaded it through `EventConnector::onLoadGame()`, then copied the 21-file slot into a second isolated data root. A fresh production process loaded that copied slot; every copied file still matches its source SHA-256 after loading. Source and copy framebuffer captures at z=70 are byte-identical. See the [save manifest](evidence/stage-00-disposable-save-manifest.txt), [source save/load trace](evidence/stage-00-disposable-source-automation.log), [copied-load trace](evidence/stage-00-disposable-copy-load-trace.log), and [source](evidence/stage-00-disposable-source-load-surface.png)/[copied](evidence/stage-00-disposable-copy-load-surface.png) world captures.
- Captured the actual `shell.loading` route during a separate production load of a fresh copy of the same 21-file Tutorial Valley save. The Loading frame displayed “Preparing kingdom”; all copied-save hashes remained unchanged. See the [capture](evidence/stage-00-route-loading-live.png), [route trace](evidence/stage-00-route-loading-live-trace.log), and [manifest](evidence/stage-00-route-loading-live-manifest.txt).
- The detached inventory capture was repeated in a second process and the PNGs were byte-identical (SHA-256 `8091d7cad157f66028c29b579024b7a843136a02fc15a9bd9d4b0700b1aab33c`).
- New Game and empty Load Game shell routes were captured through the production listener. Pause and both shell/in-game Settings surfaces were also opened on isolated roots; see [shell route run notes](evidence/stage-00-route-shell-runs.txt) and the route coverage table.
- Selected live Tutorial Valley gnome Winkle through the production selection signal and captured the detached inspector's Camera, Stats, and Expertise pages using the detached renderer. The [manifest](evidence/stage-00-route-creature-live-manifest.txt) records the isolated save copies and matching 21-file hashes.
- On another isolated 21-file save copy, focused the live Inventory report, selected `Drinks/Alcoholic/Wine/Apple` with Down, and toggled watch with Space. The controller dispatched `watch.set`; the updated category snapshot returned `watched=true` and advanced Inventory revision 2 → 3. See the [capture](evidence/stage-00-route-inventory-watch-live.png), [trace](evidence/stage-00-route-inventory-watch-live-trace.log), and [manifest](evidence/stage-00-route-inventory-watch-live-manifest.txt). All copied-save hashes remained unchanged. The events were injected into the real detached Qt window and consumed by the production RmlUi input adapter; physical input remains unverified.
- The live tile inspector probe used a separate temporary data root and a generated world with an in-memory 31×31 test patch. All 28 assertions passed. Its stockpile, contents, and active-job captures are linked from the [run manifest](evidence/stage-00-route-tile-live-manifest.txt); the root contained only logs/settings and no save files. It did not open the separate Stockpile manager. The probe's Qt events and Rml element activation are injected evidence, not physical input.
- The Agriculture manager probe used a fresh copy of the 21-file Tutorial Valley save and created one Farm field in memory. The Overview opened with ready data and stayed ready on reselection; all save-copy hashes matched afterward. See the [capture, trace, and manifest](evidence/stage-00-route-agriculture-live-manifest.txt). A separate four-plot Farm run assigned Strawberry with count two to exactly two selected plots through the production controls; both plans survived deserialization, the other plots stayed unchanged, and two live updates arrived without Refresh. The scheduler created no planting jobs. See the [scoped-plan evidence and limits](evidence/stage-00-route-agriculture-live-scoped-plan-manifest.txt). Farm settings/harvest and Grove/Pasture/empty states remain open.
- The Stockpile manager probe used another fresh copy of the 21-file Tutorial Valley save, added one in-memory field and Oak RawWood item, and opened the manager through the production tile-selection route. The one-row Stock view rendered and all save-copy hashes matched. See the [capture, trace, and manifest](evidence/stage-00-route-stockpile-live-legal-manifest.txt). Allow-list, search/sort, staged settings, hauling controls, and scope behavior remain open.
- The Workshop manager probe used a separate unchanged Tutorial Valley copy and opened the production Craft view for a probe-created Carpenter model. Its catalog, empty queue, and missing-material state rendered; the normal construction workflow and production results were not tested. See the [capture, trace, and manifest](evidence/stage-00-route-workshop-live-manifest.txt).
- The live Workshop Name draft remained outside the authoritative snapshot until Apply; the manager then showed the returned `Draft Carpenter` name. This was a probe-created model on a fresh 21-file copy whose hashes matched the verified source. See the [capture, trace, and limits](evidence/stage-00-workshop-staged-edit-manifest.txt).
- In a fresh isolated application-data root, dispatched click events on the focused production Rml elements `shell-new-setup` then `shell-back`. Route state changed `shell.main_menu` → `shell.new_game` → `shell.main_menu`; the Back action returned focus to `shell-load` rather than `shell-new-setup`. See the [trace](evidence/stage-00-command-navigation-focus-return-trace.log), [returned menu capture](evidence/stage-00-command-navigation-focus-return.png), and [manifest](evidence/stage-00-command-navigation-focus-return-manifest.txt). The focus-return defect is tracked for Stage 18 as NEW-002.
- The detached Stockpile manager rendered its actual material-filter options popup after the production toggle listener opened it; this run used synthetic fixture rows and no world save. See the [popup capture, trace, and limits](evidence/stage-00-popup-stockpile-filter-manifest.txt).
- The typed Exit confirmation opened in the primary host, canceled without leaving the main menu, then reopened and dispatched `app.exit` once. The isolated data root contained zero save files. See the [confirmation capture, trace, and limits](evidence/stage-00-confirmation-exit-manifest.txt).
- The live event-prompt modal was captured on two independent tutorial-save copies. The dialog structure repeated while its data-driven migration count differed; the [modal manifest](evidence/stage-01-modal-live-manifest.txt) records why pixel-equality is not an appropriate check.
- The original component fixture captures exposed the 560dp width/height limitation. The gallery now spans the production viewport with responsive columns; the two current [captures and generated-part traces](evidence/stage-01-component-full-manifest.txt) match byte-for-byte and show all fixture sections in one frame.
- Repeated production sentinels cover [Stockpile, Farm scope, and Settings](evidence/stage-01-repeat-sentinels-manifest.txt). The Farm runs each loaded a fresh 21-file save copy, verified exactly two of four plot plans and two live updates, left source files unchanged, and produced identical captures. Inventory fixture repeat and inspector/modal evidence are linked from that manifest.
- The source verifier passes. Three temporary-copy negative controls failed for the intended reasons: an active panel token removed while comments remained, a required component state removed, and styles targeting nonexistent `track`/`slider` scrollbar parts.
- The full registered suite ran after building its test executables: 11/13 passed. Two current baseline mismatches remain: `ui_foundation_registry` expectations for tool/action counts and `suspend_job`; `ui_foundation_mainwindow_rmlui_wiring` expects the tile-selection phrase “keep the inspector panel visible,” which its source check does not find.
- After the gallery responsive reflow and Stage 01 repeat sentinels, rebuilt the current target set and reran all 13 CTest cases: 11 passed and the same two baseline mismatches remained. No new test failure appeared.
- The application’s native windows were not available through this session’s computer-use surface, so no physical mouse/keyboard interaction is claimed.

See [baseline](01-baseline.md), [audit register](03-audit-traceability.md), [verification matrix](05-verification-matrix.md), and [Stage 01 checkpoint](stages/01-verifier.md).

## Open downstream work

- Open a normally constructed, stocked Workshop and continue Agriculture settings/harvest/Grove/Pasture/empty coverage; follow the Farm crop assignment through planting-job execution. Stockpile filter/scope/settings coverage is recorded in Stage 09. See [route coverage](stages/00-route-coverage.md).
- Keep shell focus restoration tracked for Stage 18 (NEW-002). Inventory and Stockpile automated acceptance is recorded in Stages 08 and 09; downstream manager edit semantics remain in their owning stages.
- Baseline evidence now includes representative empty/populated, selected/focused/disabled, popup/modal states on the primary and detached hosts. Add manager-specific states in their owning stages.

No normal user save was opened or changed. The current test save is reproducible from the built-in tutorial and its copied slot remains byte-identical after load.

## Next

Stage 05 shared controls and production pilots are verified: 7/7 focused tests (78 assertions), rebuilt Stage 04 regression 4/4, production Qt form checks, scale/high-contrast captures and live authoritative Stockpile edits. See the [Stage 05 checkpoint](stages/05-form-controls.md) for evidence and remaining route coverage. Stage 06 shared reports/scrolling and Inventory/Stockpile pilots are verified: 10/10 tests, 56 assertions, stable-target and generated-scrollbar checks, large-data measurements and production scale captures. See the [Stage 06 checkpoint](stages/06-reports-scrolling.md). Stage 07 shared dialogs, feedback and edit-lifecycle pilots are verified: 10/10 tests, 79 assertions, rebuilt Stage 04-06 regressions and production dialog captures. See the [Stage 07 checkpoint](stages/07-dialogs-editing.md). Stages 08-21 are complete for their automated scope; the staged plan is finished. Native physical input remains unproven.

## Session handoff - 2026-09-25 (Stage 21, plan complete)

- **Revision:** nested `Ingnomia/`, branch `feat/separate-ui-windows`. Stages 01-21 are committed together at the end of Stage 21.
- **Status:** Stage 21 VERIFIED for automated/injected scope. All stages of the plan are complete.
- **Audit items:**
  - INV-01-04 and STO-01-07 are re-verified on the migrated windows.
  - NEW-001 is fixed (visible watch check box).
  - SYS-31 is verified.
  - NEW-003 (agriculture priority not implemented in the game) remains open.
- **Commands run and results:**
  - Canonical build.
  - Stage 04-21 suites all pass.
  - Legacy suites all pass.
  - Canonical ctest 13/13.
  - Live Stockpile 35/35 and Inventory 18/18 at four scales.
  - Stage 20 menu probe 17/17.
  - Save hashes unchanged.
- **Evidence:** `evidence/stage-21/` (`stockpile/<scale>`, `inventory/<scale>`, `localization/`).
  - Stockpile runs use `INGNOMIA_AUTOMATE_STOCKPILE_OPEN_PROBE=1`, `INGNOMIA_AUTOMATE_STOCKPILE_STAGE09_LIVE=1`, `INGNOMIA_AUTOMATE_STOCKPILE_FORM_PROBE=1` and `INGNOMIA_AUTOMATE_STOCKPILE_STAGE21_DIR`.
  - Inventory runs use `INGNOMIA_AUTOMATE_STAGE21_INVENTORY=1` and `INGNOMIA_AUTOMATE_STAGE21_DIR`.
- **Follow-up work (outside the plan):**
  - Move text built at run time into the catalog.
  - Verify layouts with the qps-long pseudo-locale (30 percent expansion).

## Session handoff - 2026-09-25 (Stages 0-9 review)

- **Status:** follow-up fixes after Stage 21, not yet committed. HUD menu access keys, the list view heading offset and the retired inventory probes are fixed; the Codex-era review fixed drop-down insets, legacy ID rules, caption capitalization, wizard coverage, the tile heading, the Inventory frame, Load Game's Cancel and the clipped Settings label. The game window now draws a Windows 98 frame and caption instead of the native one. Details: [Stage 21 follow-up](stages/21-cleanup-conformance.md#follow-up-review-of-stages-0-9-2026-09-25).
- **Commands run and results:** canonical build; Stage 04-21 suites all pass; legacy suites all pass; canonical ctest 13/13; all 12 live probes pass at 1x (Stage 17 also at 2x, with the frame checks) with save hashes unchanged.
- **Follow-up work:** move text built at run time into the catalog; verify layouts with qps-long; re-run the live probes at 125/150/200%; test physical input and the Windows High Contrast switch; resolve the MS W98 UI font licence before release.
